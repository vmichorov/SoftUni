function solve(firstName, lastName, age, town) {
    firstName = firstName;
    lastName = lastName;
    age = Number(age);
    town = town;

    console.log(`You are ${firstName} ${lastName}, a ${age}-years old person from ${town}.`)
}

solve("Vasil", "Michorov", "18", "Haskovo")