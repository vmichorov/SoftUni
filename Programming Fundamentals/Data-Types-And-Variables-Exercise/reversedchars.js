function reversedChars(par1, par2, par3) {
    let first = par1;
    let second = par2;
    let third = par3;
    let reversedString = `${third} ${second} ${first}`;
    console.log(reversedString);
}

reversedChars(`A`, `B`, `C`);